// Insert 'debugger' into code
// Run 'node inspect <filename.js>' and type 'cont' until you hit debugger
// Type 'repl' to activate debugger

function add(x, y) {
  let total = x + y;
  debugger
}

add(2,3);
